'use strict';

const app = require('../app');

const newGameSuccess = (data) => {
};

const failure = (error) => {
};

const updateGameSuccess = () => {
};

module.exports = {
  newGameSuccess,
  failure,
  updateGameSuccess
};
